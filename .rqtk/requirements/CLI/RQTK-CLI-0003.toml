id = "RQTK-CLI-0003"
title = "Dry runs"
category = "CLI"
type = "Functional"
state = "Approved"
priority = "Critical"
statement = "Every rqtk command that writes files shall accept `--dry-run` and then write nothing."
rationale = "Agents and people preview changes before making them."

[trace]
parents = ["RQTK-SYS-0003"]

[verification]
method = "Test"
level = "System"
phase = "Release"
success_criteria = "Dry runs report their changes and leave the repository byte-for-byte unchanged."

[[verification.activities]]
id = "VA-CLI-003-01"
name = "Dry runs write nothing"
