id = "RQTK-CORE-0003"
title = "Semantic content hash"
category = "CORE"
type = "Functional"
state = "Approved"
priority = "Critical"
statement = "rqtk-core shall change a requirement's content hash when its statement, links, parameters or verification method change, and only then."
rationale = "The content hash decides when evidence becomes Suspect, so it must track meaning and ignore administration."

[trace]
parents = ["RQTK-SYS-0002"]

[verification]
method = "Test"
level = "Unit"
phase = "Release"
success_criteria = "Semantic edits change the hash; administrative edits do not; distinct field sets never collide."

[[verification.activities]]
id = "VA-CORE-003-01"
name = "Hash tracks semantic fields only"
