[package]
name = "rqtk-export"
version = "0.1.0"
edition = "2024"

[dependencies]
rqtk-core = { path = "../rqtk-core" }
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2"
clap = { version = "4.5", features = ["derive"], optional = true }

[features]
clap = ["dep:clap"]
