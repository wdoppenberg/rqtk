{
  "$defs": {
    "ActivityEvidence": {
      "description": "The latest recorded result for one verification activity.\n\nUnknown fields are ignored so that files written by a later rqtk still load.",
      "properties": {
        "commit": {
          "description": "Commit checked out when the evidence was recorded.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "Verification activity ID.",
          "type": "string"
        },
        "outcome": {
          "$ref": "#/$defs/Outcome"
        },
        "requirement": {
          "$ref": "#/$defs/RequirementId",
          "description": "Requirement that owned the activity when the evidence was recorded."
        },
        "requirement_hash": {
          "description": "The requirement's content hash when the evidence was recorded. If the requirement's\ncurrent hash differs, the evidence is stale and the activity is Suspect.",
          "type": "string"
        },
        "tests": {
          "description": "Test cases that produced the outcome, as `classname::name`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tests_hash": {
          "description": "Hash of the linked tests' source when they last ran.",
          "type": [
            "string",
            "null"
          ]
        },
        "unchanged_tests": {
          "description": "The requirement was re-verified after it changed, by the same tests that passed for\nits earlier wording. It stays Suspect until someone runs `rqtk review`.",
          "type": "boolean"
        },
        "upstream": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Content hashes of the requirement's ancestors and of the needs they satisfy, when this\nversion of the requirement was first verified. A later change to any of them makes\nthe requirement Suspect until it is reviewed.",
          "type": "object"
        }
      },
      "required": [
        "id",
        "requirement",
        "requirement_hash",
        "outcome",
        "tests"
      ],
      "type": "object"
    },
    "Outcome": {
      "enum": [
        "passed",
        "failed"
      ],
      "type": "string"
    },
    "RequirementId": {
      "description": "Identifier of a requirement, e.g. `FOBC-SYS-0001`.",
      "type": "string"
    },
    "Review": {
      "description": "A person's or agent's confirmation that a requirement still holds after something it\ndepends on changed, recorded with `rqtk review`.",
      "properties": {
        "commit": {
          "description": "Commit checked out when the review was recorded.",
          "type": [
            "string",
            "null"
          ]
        },
        "date": {
          "description": "Date of the review, `YYYY-MM-DD`.",
          "type": "string"
        },
        "note": {
          "type": [
            "string",
            "null"
          ]
        },
        "requirement": {
          "$ref": "#/$defs/RequirementId"
        },
        "requirement_hash": {
          "description": "The requirement's content hash when it was reviewed; a later change voids the review.",
          "type": "string"
        },
        "upstream": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Content hashes of its ancestors and their needs when it was reviewed.",
          "type": "object"
        }
      },
      "required": [
        "requirement",
        "requirement_hash",
        "date"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "activity": {
      "default": [],
      "items": {
        "$ref": "#/$defs/ActivityEvidence"
      },
      "type": "array"
    },
    "review": {
      "items": {
        "$ref": "#/$defs/Review"
      },
      "type": "array"
    },
    "schema_version": {
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "written_by": {
      "description": "Version of rqtk that last wrote the file.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "schema_version"
  ],
  "title": "EvidenceFile",
  "type": "object"
}
